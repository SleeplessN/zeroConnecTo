{
  "scripts": {
    "start": "run-p watch serve",
    "sass": "sass --load-path=node_modules src/scss:src/css",
    "nosource": "npm run sass -- --no-source-map",
    "watch": "npm run sass -- --watch",
    "serve": "live-server ./src"
  },
  "devDependencies": {
    "@euid/scss-mixins": "^0.2.0",
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "sass": "^1.54.0"
  }
}
